<!DOCTYPE html>
<html lang="fr" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio Pro</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-slate-50 text-slate-900 font-sans">
    <% 
// Sécurité : si path n'existe pas, on le définit sur une chaîne vide
if (typeof path === 'undefined') { var path = ''; } 
%>

<nav class="bg-blue-600 shadow-lg sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
        <a href="/" class="text-2xl font-bold text-white flex items-center gap-2">
            <i class="fas fa-code"></i> Portfolio.dev
        </a>
        
        <div class="hidden md:flex space-x-4 text-sm font-medium items-center">
            <a href="/" class="px-5 py-2.5 rounded-full transition <%= (path === '/' || path === '') ? 'bg-white text-blue-600 shadow-md' : 'text-white hover:bg-blue-500' %>">
                Accueil
            </a>
            <a href="/services" class="px-5 py-2.5 rounded-full transition <%= path === '/services' ? 'bg-white text-blue-600 shadow-md' : 'text-white hover:bg-blue-500' %>">
                Services
            </a>
            <a href="/projets" class="px-5 py-2.5 rounded-full transition <%= path === '/projets' ? 'bg-white text-blue-600 shadow-md' : 'text-white hover:bg-blue-500' %>">
                Projets
            </a>
            <a href="/contact" class="px-5 py-2.5 rounded-full transition <%= path === '/contact' ? 'bg-white text-blue-600 shadow-md' : 'text-white hover:bg-blue-500' %>">
                Me contacter
            </a>
        </div>
    </div>
</nav>