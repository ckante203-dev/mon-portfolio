<%- include('partials/header') %>

<header class="py-24 px-4 bg-white border-b border-slate-100">
    <div class="max-w-4xl mx-auto text-center">
        <span class="inline-block px-4 py-1.5 mb-6 text-sm font-semibold tracking-wide text-blue-600 uppercase bg-blue-50 rounded-full">
            Disponible pour de nouveaux projets
        </span>
        <h1 class="text-5xl md:text-6xl font-extrabold mb-6 tracking-tight leading-tight text-slate-900">
            Développeur de solutions <span class="text-blue-600">digitales</span> sur mesure
        </h1>
        <p class="text-xl text-slate-600 mb-10 leading-relaxed max-w-2xl mx-auto">
            Je conçois et développe des applications web performantes en utilisant les dernières technologies. 
            Expertise **Node.js**, **Express** et **Supabase**.
        </p>
        <div class="flex flex-col sm:flex-row justify-center gap-4">
            <a href="/projets" class="px-8 py-4 bg-blue-600 text-white rounded-xl font-bold hover:bg-blue-700 transition shadow-lg shadow-blue-200">Voir mes travaux</a>
            <a href="/services" class="px-8 py-4 bg-white text-slate-900 border border-slate-200 rounded-xl font-bold hover:bg-slate-50 transition">Mes services</a>
        </div>
    </div>
</header>

<section class="max-w-6xl mx-auto px-4 py-24">
    <div class="text-center mb-16">
        <h3 class="text-3xl font-bold mb-4">Mes Services</h3>
        <p class="text-slate-500">Des solutions adaptées à vos besoins business.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <% if (services && services.length > 0) { %>
            <% services.forEach(service => { %>
                <article class="group bg-white p-8 rounded-3xl shadow-sm border border-slate-100 hover:border-blue-200 hover:shadow-xl hover:-translate-y-2 transition duration-300">
                    <div class="w-14 h-14 bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center mb-6 text-2xl group-hover:bg-blue-600 group-hover:text-white transition-colors duration-300">
                        <i class="fas <%= service.icone || 'fa-rocket' %>"></i>
                    </div>
                    <h4 class="text-xl font-bold mb-3"><%= service.titre %></h4>
                    <p class="text-slate-600 leading-relaxed mb-6">
                        <%= service.description %>
                    </p>
                    <a href="/services" class="text-blue-600 font-bold text-sm inline-flex items-center gap-2 group-hover:gap-3 transition-all">
                        Détails du service <i class="fas fa-arrow-right"></i>
                    </a>
                </article>
            <% }); %>
        <% } else { %>
            <p class="col-span-full text-center text-slate-400">Aucun service disponible.</p>
        <% } %>
    </div>
</section>

<%- include('partials/footer') %>